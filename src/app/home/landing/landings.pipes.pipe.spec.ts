import { LandingsPipesPipe } from './landings.pipes.pipe';

describe('LandingsPipesPipe', () => {
  it('create an instance', () => {
    const pipe = new LandingsPipesPipe();
    expect(pipe).toBeTruthy();
  });
});
